スタート：画面を表示する
状態の定義(const useState)
  メモ：
    変数定義：
    let name = "shohei"
    変数の変更：
    name = "sota"

    Reactの場合：
    const [name, setName] = React.useState("shohei")
    setName("sota")

  mail, password

状態の変更(inputとstateの同期)
  onChange((引数) => {処理})
  onChange(() => {console.log("hello, React!")})


途中のゴール：Fetchでバックエンドにリクエストを投げる
  {
    mail: mail, ← inputに打ち込まれた値を代入する必要
    password: password
  }

どんなときに： onChangeイベントが発生したときに
どんな処理をするか： console.log("hello, React")を実行する
UIにはどんな変更をするか： 何もしない

どんなときに： onChangeイベントが発生したときに
どんな処理をするか： mailの箱にinputの内容を格納する

どんなときに： onClickイベントが発生したときに
どんな処理をするか： mailとPasswordの箱を一緒にしてBEに投げる

UIにはどんな変更をするか： mailの内容を表示する



monitorEvents(document)




最終ゴール：バックエンドからのレスポンスがOKの場合、Atomにデータを保存して、会員一覧画面に遷移する